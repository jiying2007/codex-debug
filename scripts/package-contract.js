#!/usr/bin/env node
'use strict';
const assert=require('node:assert/strict');const {execFileSync}=require('node:child_process');const path=require('node:path');const root=path.resolve(__dirname,'..');const stdout=execFileSync(process.platform==='win32'?'npm.cmd':'npm',['pack','--dry-run','--json','--ignore-scripts'],{cwd:root,encoding:'utf8',stdio:['ignore','pipe','inherit'],maxBuffer:8*1024*1024}),records=JSON.parse(stdout),record=records[0];assert.ok(record&&Array.isArray(record.files),'npm pack --dry-run JSON did not contain files');const files=record.files.map(x=>String(x.path).replace(/\\/g,'/')).sort();for(const required of ['package.json','extension.js','src/cli.js','src/debug.js','src/contracts.js','src/codex-safe-core/index.js','README.md','README.zh-CN.md','ARCHITECTURE.md','SECURITY.md','ROADMAP.md','product-contract.json'])assert.ok(files.includes(required),`package missing required file: ${required}`);for(const file of files){assert.doesNotMatch(file,/^(?:test|quality|scripts|\.github|\.codex-debug|\.git)(?:\/|$)/,`package contains development/private surface: ${file}`);}assert.ok(files.length<150,'package unexpectedly broad');process.stdout.write(JSON.stringify({fileCount:files.length,unpackedSize:record.unpackedSize||0,forbiddenDevelopmentSurfaces:false})+'\n');
