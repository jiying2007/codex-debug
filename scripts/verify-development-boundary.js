#!/usr/bin/env node
'use strict';
const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');
function walk(dir){if(!fs.existsSync(dir))return[];return fs.readdirSync(dir,{withFileTypes:true}).flatMap(entry=>{const p=path.join(dir,entry.name);return entry.isDirectory()?walk(p):[p];});}
function verifyDevelopmentBoundary(){const contract=JSON.parse(fs.readFileSync(path.join(root,'product-contract.json'),'utf8')),pkg=JSON.parse(fs.readFileSync(path.join(root,'package.json'),'utf8'));assert.equal(contract.lifecycle,'development','this gate is intentionally scoped to the development lifecycle');const workflows=walk(path.join(root,'.github','workflows')).filter(file=>/\.ya?ml$/i.test(file));for(const file of workflows){const rel=path.relative(root,file).replace(/\\/g,'/'),text=fs.readFileSync(file,'utf8');assert.doesNotMatch(path.basename(file),/(?:release|publish|marketplace)/i,`development lifecycle cannot contain release workflow: ${rel}`);for(const forbidden of [/\bpull_request_target\s*:/i,/\bcontents\s*:\s*write\b/i,/\bpackages\s*:\s*write\b/i,/\bid-token\s*:\s*write\b/i,/\bnpm\s+publish\b/i,/\bvsce\s+publish\b/i,/\bovsx\s+publish\b/i,/\bgh\s+release\b/i,/\bgit\s+push\b/i])assert.doesNotMatch(text,forbidden,`development workflow contains publication/write surface: ${rel}`);}const scripts=JSON.stringify(pkg.scripts||{});for(const forbidden of [/npm\s+publish/i,/vsce\s+publish/i,/ovsx\s+publish/i,/gh\s+release/i,/git\s+push/i])assert.doesNotMatch(scripts,forbidden,'development package scripts cannot publish or push');return {lifecycle:contract.lifecycle,workflowCount:workflows.length,publicationSurfaces:false};}
if(require.main===module){try{process.stdout.write(`${JSON.stringify(verifyDevelopmentBoundary())}\n`);}catch(error){console.error(error.stack||error.message);process.exitCode=2;}}
module.exports={verifyDevelopmentBoundary};
