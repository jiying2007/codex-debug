#!/usr/bin/env node
'use strict';
const {parseArgs,help}=require('./args');
const {runDebugSession,resumeDebugSession,applyDebugSessionPatch,rollbackDebugSession}=require('./debug');
async function readStdin(){if(process.stdin.isTTY)return '';const chunks=[];for await(const chunk of process.stdin)chunks.push(chunk);return Buffer.concat(chunks.map(c=>Buffer.isBuffer(c)?c:Buffer.from(c))).toString('utf8');}
async function main(){const options=parseArgs(process.argv.slice(2));if(options.help){process.stdout.write(help());return;}let result,action='debug';if(options.applySession){action='apply-session';result=await applyDebugSessionPatch({...options,sessionId:options.applySession});}else if(options.rollbackSession){action='rollback-session';result=rollbackDebugSession({...options,sessionId:options.rollbackSession});}else if(options.resume){action='resume';result=await resumeDebugSession(options);}else{if(!options.logFile&&!options.command&&!options.coreFile)options.text=await readStdin();if(!options.logFile&&!options.command&&!options.coreFile&&!String(options.text||'').trim()&&!options.artifacts.length)throw new Error('Provide --log, --command, --core/--executable, --artifact, or pipe failure evidence on stdin.');result=await runDebugSession(options);}if(action==='rollback-session'){process.stdout.write(`${JSON.stringify({action,...result},null,2)}\n`);return;}process.stdout.write(`${JSON.stringify({action,sessionId:result.sessionId,parentSessionId:result.lineage?.parentSessionId||null,fixStatus:result.fixStatus,failureKind:result.evidence.kind,rootCause:result.investigation.rootCause,confidence:result.investigation.confidence,bisect:result.evidence.bisect||null,recommendedTests:result.evidence.regressionTests?.recommendedTests||[],patch:result.patch,verification:result.verification,sessionPath:result.sessionPath||'',receipt:result.receipt},null,2)}\n`);}
if(require.main===module)main().catch(error=>{process.stderr.write(`Codex Debug Safe failed: ${error.message}\n`);process.exitCode=2;});
module.exports={main,readStdin};
