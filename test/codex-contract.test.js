'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const {normalizeCausalVerification,applyCausalVerification}=require('../src/codex');
const draft={rootCause:'race',confidence:.9,hypotheses:[{id:'H1',status:'supported',title:'race',claim:'race',evidenceFor:[],evidenceAgainst:[],causalAnchors:[],verificationSteps:[]}],patch:{summary:'fix',unifiedDiff:'diff --git a/a b/a',rationale:'x',risk:'low'},verificationPlan:[],coverageGaps:[]};
test('causal verifier fails closed on unsupported patch',()=>{const v=normalizeCausalVerification({rootCauseAssessment:'insufficient',confidence:.7,hypotheses:[{id:'H1',status:'open',evidence:[],reason:'missing lock trace'}],patchDisposition:'accept',patchRisks:[],coverageGaps:['no lock state']},draft);assert.equal(v.patchDisposition,'reject');const merged=applyCausalVerification(draft,v);assert.equal(merged.patch,null);assert.ok(merged.coverageGaps.includes('draft_patch_rejected_by_causal_verifier'));});
test('causal verifier can accept only supported draft',()=>{const v=normalizeCausalVerification({rootCauseAssessment:'supported',confidence:.8,hypotheses:[{id:'H1',status:'supported',evidence:['TSAN edge'],reason:'matches source lifetime'}],patchDisposition:'accept',patchRisks:[],coverageGaps:[]},draft),merged=applyCausalVerification(draft,v);assert.ok(merged.patch);assert.equal(merged.confidence,.8);});
