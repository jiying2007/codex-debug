'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {makeSessionId,createDebugReceipt,validateReceipt,validateStoredSession}=require('../src/contracts');test('receipt binds evidence investigation ledger verification and patch state',()=>{const evidence={digest:'a'.repeat(64),git:{head:'deadbeef'},source:{label:'fixture'},workspace:'x'},investigation={rootCause:'bad lifetime',confidence:.8,hypotheses:[],verificationPlan:[]},sessionId=makeSessionId(evidence.digest,'2026-01-01T00:00:00.000Z'),ledger={},verification={status:'not-run'},patch={checked:true,applied:false,paths:['a.c']},receipt=createDebugReceipt({sessionId,evidence,investigation,ledger,verification,patchResult:patch,fixStatus:'diagnosed',createdAt:'2026-01-01T00:00:00.000Z'}),session={sessionId,evidence,investigation,ledger,verification,patch,fixStatus:'diagnosed',receipt};assert.equal(validateReceipt(receipt).fixStatus,'diagnosed');assert.match(receipt.patchStateDigest,/^[0-9a-f]{64}$/);assert.equal(validateStoredSession(session).sessionId,sessionId);assert.throws(()=>validateStoredSession({...session,patch:{...patch,applied:true}}),/stale or modified/);});
