'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const os=require('node:os');const fs=require('node:fs');const path=require('node:path');const {runDebugSession,determineFixStatus}=require('../src/debug');
test('deterministic session cannot claim verified without verification',async()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'codex-debug-test-'));try{const r=await runDebugSession({workspace:dir,text:'ERROR: AddressSanitizer: heap-use-after-free',deterministicOnly:true,persist:false});assert.equal(r.evidence.kind,'sanitizer');assert.equal(r.fixStatus,'diagnosed');assert.notEqual(r.ledger.entries[0].status,'confirmed');}finally{fs.rmSync(dir,{recursive:true,force:true});}});
test('fix status is deterministic',()=>{assert.equal(determineFixStatus({investigation:{rootCause:'x',patch:{}},patchResult:{applied:true},verification:{status:'not-run'}}),'applied-unverified');assert.equal(determineFixStatus({investigation:{},verification:{status:'passed'}}),'verified');});
