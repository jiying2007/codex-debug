'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');const {execFileSync}=require('node:child_process');const {runDebugSession,resumeDebugSession,applyDebugSessionPatch,rollbackDebugSession}=require('../src/debug');const {createDebugReceipt,makeSessionId}=require('../src/contracts');const {writeSession}=require('../src/session-store');
function init(){const dir=fs.mkdtempSync(path.join(os.tmpdir(),'codex-debug-e2e-'));execFileSync('git',['init','-q'],{cwd:dir});execFileSync('git',['config','user.name','fixture'],{cwd:dir});execFileSync('git',['config','user.email','fixture@example.invalid'],{cwd:dir});fs.writeFileSync(path.join(dir,'state.txt'),'bad\n');fs.writeFileSync(path.join(dir,'reproduce.js'),"const fs=require('node:fs');const bad=fs.readFileSync('state.txt','utf8').trim()==='bad';if(bad)console.error('fatal error: deterministic fixture');process.exit(bad?1:0);\n");execFileSync('git',['add','.'],{cwd:dir});execFileSync('git',['commit','-qm','fixture baseline'],{cwd:dir});return dir;}
function patchText(){return 'diff --git a/state.txt b/state.txt\n--- a/state.txt\n+++ b/state.txt\n@@ -1 +1 @@\n-bad\n+good\n';}
function withFixturePatch(first,dir){const unifiedDiff=patchText(),investigation={...first.investigation,rootCause:'fixture state regression',confidence:.9,hypotheses:[{id:'H1',title:'fixture regression',claim:'bad state causes failure',status:'supported',evidenceFor:['fixture'],evidenceAgainst:[],causalAnchors:['state.txt'],verificationSteps:['run reproduce.js']}],patch:{summary:'fix fixture state',unifiedDiff,rationale:'fixture-only deterministic patch',risk:'low'},causalVerification:{rootCauseAssessment:'supported',confidence:.9,hypotheses:[{id:'H1',status:'supported',evidence:['fixture'],reason:'fixture'}],patchDisposition:'accept',patchRisks:[],coverageGaps:[]}},patch={checked:true,applied:false,paths:['state.txt']},fixStatus='proposed',sessionId=makeSessionId(first.evidence.digest),lineage={parentSessionId:first.sessionId,parentDebugFingerprint:first.receipt.debugFingerprint,proposedAt:new Date().toISOString()},receipt=createDebugReceipt({sessionId,evidence:first.evidence,investigation,ledger:first.ledger,verification:first.verification,patchResult:patch,lineage,fixStatus,model:'fixture',codexVersion:''}),stored={...first,sessionId,fixStatus,investigation,patch,lineage,receipt};stored.sessionPath=writeSession(dir,stored);return stored;}
function withUnauthorizedFixturePatch(first,dir){const unifiedDiff=patchText(),investigation={...first.investigation,rootCause:'unverified fixture guess',confidence:.4,patch:{summary:'unsafe guess',unifiedDiff,rationale:'no supported causal proof',risk:'high'},causalVerification:{rootCauseAssessment:'insufficient',confidence:.4,hypotheses:[],patchDisposition:'reject',patchRisks:['unsupported'],coverageGaps:['missing causal proof']}},patch={checked:true,applied:false,paths:['state.txt']},fixStatus='proposed',sessionId=makeSessionId(first.evidence.digest),lineage={parentSessionId:first.sessionId,parentDebugFingerprint:first.receipt.debugFingerprint,proposedAt:new Date().toISOString()},receipt=createDebugReceipt({sessionId,evidence:first.evidence,investigation,ledger:first.ledger,verification:first.verification,patchResult:patch,lineage,fixStatus,model:'fixture',codexVersion:''}),stored={...first,sessionId,fixStatus,investigation,patch,lineage,receipt};writeSession(dir,stored);return stored;}
test('observed failing baseline plus manual mutation plus resumed green runs verifies fix without inventing root-cause confirmation',async()=>{const dir=init();try{const first=await runDebugSession({workspace:dir,command:'node reproduce.js',reproRuns:2,deterministicOnly:true,persist:true});assert.equal(first.evidence.reproduction.failures,2);assert.equal(first.evidence.reproduction.reproducibleFailure,true);assert.notEqual(first.fixStatus,'verified');assert.ok(first.sessionPath);fs.writeFileSync(path.join(dir,'state.txt'),'good\n');const resumed=await resumeDebugSession({workspace:dir,resume:first.sessionId,verifyCommand:'node reproduce.js',verifyRuns:3,persist:true});assert.equal(resumed.verification.status,'passed');assert.equal(resumed.verification.failureTransition.status,'resolved');assert.equal(resumed.verification.mutationObserved,true);assert.equal(resumed.verification.afterSummary.passes,3);assert.equal(resumed.fixStatus,'verified');assert.equal(resumed.lineage.parentSessionId,first.sessionId);assert.equal(resumed.ledger.entries.some(h=>h.status==='confirmed'),false);}finally{fs.rmSync(dir,{recursive:true,force:true});}});
test('stored verifier-accepted patch applies with snapshot, rolls back, and refuses rollback after drift',async()=>{const dir=init();try{const target=path.join(dir,'state.txt'),first=await runDebugSession({workspace:dir,command:'node reproduce.js',reproRuns:2,deterministicOnly:true,persist:true}),before=fs.readFileSync(target),proposed=withFixturePatch(first,dir);const applied=await applyDebugSessionPatch({workspace:dir,sessionId:proposed.sessionId});assert.equal(applied.fixStatus,'applied-unverified');assert.equal(applied.lineage.parentSessionId,proposed.sessionId);assert.ok(applied.patch.snapshotId);assert.equal(fs.readFileSync(target,'utf8').trim(),'good');const rolled=rollbackDebugSession({workspace:dir,sessionId:applied.sessionId});assert.equal(rolled.rolledBack,true);assert.deepEqual(fs.readFileSync(target),before);const proposedAgain=withFixturePatch(first,dir),appliedAgain=await applyDebugSessionPatch({workspace:dir,sessionId:proposedAgain.sessionId});fs.writeFileSync(target,'manual\n');assert.throws(()=>rollbackDebugSession({workspace:dir,sessionId:appliedAgain.sessionId}),/workspace drifted after patch/);assert.equal(fs.readFileSync(target,'utf8').trim(),'manual');}finally{fs.rmSync(dir,{recursive:true,force:true});}});
test('apply entrypoint rejects a receipt-valid patch without supported causal authorization',async()=>{const dir=init();try{const target=path.join(dir,'state.txt'),first=await runDebugSession({workspace:dir,command:'node reproduce.js',reproRuns:2,deterministicOnly:true,persist:true}),unauthorized=withUnauthorizedFixturePatch(first,dir);await assert.rejects(()=>applyDebugSessionPatch({workspace:dir,sessionId:unauthorized.sessionId}),error=>error?.code==='EDEBUGPATCHAUTH');assert.equal(fs.readFileSync(target,'utf8').trim(),'bad');}finally{fs.rmSync(dir,{recursive:true,force:true});}});
